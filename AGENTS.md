# AGENTS.md — professoressa-project

## Before you do anything

When a new session starts, ask the user if they want to run the following command in the relevant repo, **do not run it automatically**:

```bash
git pull
```

Always pull before starting a session to make sure you are working on the latest version.
